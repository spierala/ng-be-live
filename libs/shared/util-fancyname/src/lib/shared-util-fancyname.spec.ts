import { sharedUtilFancyname } from './shared-util-fancyname';

describe('sharedUtilFancyname', () => {
  it('should work', () => {
    expect(sharedUtilFancyname()).toEqual('shared-util-fancyname');
  });
});
